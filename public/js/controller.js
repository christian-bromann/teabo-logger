define([
    'jquery',
    'underscore'
], function( $, _) {
    
    var LoggerController = function(options) {

    };

    LoggerController.prototype = {
        
    };

    return LoggerController;
});
